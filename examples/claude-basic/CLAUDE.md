# Claude Basic Profile

This is a minimal Claude Code setup managed by coffeeenv.

- The fake `$HOME` for `claude` is `${COFFEEENV_HOME}`.
- The real shell `$HOME` is still your normal one — git, ssh etc. work as usual.
